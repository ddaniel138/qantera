import { formatNumber } from "./number";
import { formatAmount, formatDate, shortenHash } from "./faucetFormat";
import { saveFaucetHistory, getFaucetHistory } from "./faucetHistory";
import { formatCountdown, getClaimInfo } from "./faucetClaim";
import { formatRelativeTime } from "./formatTime";
import { formatAddress } from "./formatAddress";

export {
    formatNumber,
    formatAmount,
    formatDate,
    shortenHash,
    saveFaucetHistory,
    getFaucetHistory,
    formatCountdown,
    getClaimInfo,
    formatRelativeTime,
    formatAddress,
}